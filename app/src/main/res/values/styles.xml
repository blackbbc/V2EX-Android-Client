<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Apptheme.Base"></style>
    <style name="Apptheme.Base" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->

        <item name="android:colorPrimary">@color/accent_material_dark</item>
        <item name="android:colorPrimaryDark">@color/accent_material_light</item>
        <item name="android:windowBackground">@color/dim_foreground_disabled_material_dark</item>
    </style>

</resources>
